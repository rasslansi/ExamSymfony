{% extends 'template2.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 style="text-align: center;margin-bottom: 30px;margin-top: 80px">Ajouter un étudiant</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4 col-md-offset-4"></div>
            <div class="col-md-4 col-md-offset-4" >
                <form action="{{ path('etudiant.add') }}" method="post" style="margin-bottom: 30px">
                    {{ form_widget(form) }}
                    <div style="text-align: center">
                        <button type="submit" class="btn btn-primary">Ajouter</button>
                    </div>
                </form>
                <div class="col-md-12" style="text-align: center;margin-left: 5px">
                    <a href="{{ path('etudiant.list') }}" class="btn btn-secondary btn-lg">show list</a>
                </div>
            </div></div>
    </div>

{#    {{ form(form) }}#}
{% endblock %}